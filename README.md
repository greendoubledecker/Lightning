I used a best-first search (a bit like breadth-forst search) to simulate the lightning's path.
The colours of the air pixels represent the random weights given to them, lighter = harder for lightning to cross, darker = easier.
Once lightning hits the red lightning rod, a 'bolt' will be traced back to the start.
